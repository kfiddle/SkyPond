const menuStrings = ["one nifty link", "another cool link", "yet another", "going for one more",
    "you get the point", "instagram", "some other site"];

const sideBarMenuItems = [];

for (let h = 0; h < menuStrings.length; h++) {
    let menuItem = document.createElement('h3');
}
